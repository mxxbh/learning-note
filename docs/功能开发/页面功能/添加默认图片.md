# 添加默认图片

给 `<img>` 元素指定默认图片。

## 方式一

通过监听 `onerror` 事件，在图片加载异常时显示默认图片。

```html
<!-- onerror 事件触发时，修改图片地址，并清除 onerror 事件避免死循环 -->
<img src="<原图片地址>" onerror="this.onerror = ''; this.src='<默认图片地址>'" />
```
