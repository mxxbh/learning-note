# 优化性能

优化页面性能，加快页面渲染速度、相应速度。

## 方式一

使用 `transform`、`opacity`、`filter` 等 CSS 属性启用 GPU 硬件加速，避免使用会导致页面重排的属性。

## 方式二

使用 [will-change](https://developer.mozilla.org/zh-CN/docs/Web/CSS/will-change) 属性。

::: warning
`will-change` 只是一个提示，浏览器不一定会按照提示进行优化。
:::
